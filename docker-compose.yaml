services:
  mqtt:
    image: eclipse-mosquitto:latest
    container_name: mqtt-broker
    ports:
      - "1883:1883"
    volumes:
      - ./config:/config
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log
    restart: unless-stopped